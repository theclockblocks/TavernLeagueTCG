-- Tavern League TCG: license-layer enforcement (Challenge/League) -
-- gear-slot locks with auto-unequip, spellbook graying, action-bar
-- tinting, cast callouts, talent gating, bag locks. Ported from
-- DeckLocked's Enforce.lua; reads state through the TT.Run() accessors.
--
-- v0.5.0 scaffolding stub - the port lands in a later commit.

local ADDON, TT = ...
